/*
  Uma universidade avalia os seus esturantes da seguinte forma:
    - Cada estudante recebe uma nota de 0 a 100
    - Notas menores do que 50 são reprovadas
    - As notas são arredondadas para múltiplos de 5 (ex: 50, 55, 60)
      desde que a diferença seja de no máximo 2 pontos.
      Por exemplo, 58 é arredondada para 60, mas 57 não.
   
  Modifique a função "gradingStudents" para que retorne *apenas
  os estudantes aprovados*, no seguinte formato:
    - Uma propriedade "name", da mesma forma que recebida.
    - Uma propriedade "finalScore" com a nota arredondada.
*/


export default function gradingStudents (students) {
  return students
}

